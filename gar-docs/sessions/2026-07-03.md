# Session — 2026-07-03

## Shipped
- **Forest_A segment fixed to treadmill convention** — re-oriented to identity so the 500-stud floor runs along world **+Z** (was yawed −90°), anchored all 412 parts, and set the model pivot to the **entry (−Z) face** at floor-top height. Matches what `ProcGenManager` expects (`clone:PivotTo(entryPos)`, exit = pivot + (0,0,500)).
- **ServerStorage template populated** — `ServerStorage.SegmentTemplates.Forest.Forest_A` now holds the full authored geometry (255 geometry children / 412 parts). It was floor-only before, so runs would have spawned an empty floor.
- **RunCorridorOrigin marker added** at `(0, 100, 0)` (anchored, transparent, non-colliding) so ProcGen stops using its `(0,100,0)` fallback. Its Y is the corridor floor altitude.
- **Spawn aligned to the corridor** — `SpawnLocation` moved to `(0, 103, −12)` facing **+Z**; the Workspace `Forest_A` authoring copy re-pivoted to `(0,100,0)` so the track visually starts just ahead of the spawn.
- **Flight forward-lock tuned** — in `src/StarterPlayerScripts/Client.client.lua`: `MaxYawDeviation` 75 → 90 (glider can now steer to full lateral, Vz=0) and `currentVelZ` floored at 0 (hard invariant: never travel backward down the corridor toward −Z / away from the rings).
- **Plugin config** — set `GAR_REPO_PATH` in `gar-config.md` to `C:\Users\karso\Desktop\Glide-A-Rot!` so gar-docs auto-loads on this machine.

## In Progress
- **Persist + commit the map work** — all geometry/marker/spawn changes live only in `HangglideARot.rbxlx` (ServerStorage isn't in the Rojo tree; Forest_A/SpawnLocation are `$ignoreUnknownInstances`). Next step: **Ctrl+S in Studio** to write the place, then commit. Blocked by a stale `.git/index.lock`.
- **Flight physics not yet live in Studio** — the running Studio session has an *older* `Client.client.lua` without the forward-lock. The disk/source already had the `YawForwardLock` scaffold; Rojo needs to re-sync (or restart) for the change to take effect in-session.
- **Forest_B / Forest_C** — still floor-only stubs; only Forest_A is authored. Same orientation/pivot/anchor recipe applies.
- **First ProcGen playtest** — still pending.

## Decisions Made
- **Segment authoring convention affirmed** (see `decisions/2026-07-03-segment-authoring-convention.md`): +Z corridor, identity orientation, pivot at entry (−Z) face, all parts anchored.
- **Side terrain must be Parts/MeshParts, not Roblox voxel Terrain** — the treadmill recycles geometry with `PivotTo`, and voxel Terrain is a singleton that can't be cloned/shifted per-segment. Flank the floor with anchored parts inside each segment's `Geometry` folder so scenery recycles with the run.
- **Flight steering policy** — lateral turn capped at ±90° off +Z; never reverse.

## Blockers / Open Questions
- **Stale `.git/index.lock`** — a git process on the machine holds it; the sandbox can't remove it (permission denied). Must be cleared before any commit.
- **Player ↔ corridor coupling gap** — ProcGen builds the window at `RunCorridorOrigin` on glider deploy, but nothing teleports the player there, and the client heading isn't tied to the marker. Current workaround: spawn placed at the origin facing +Z so the player naturally launches onto segment 1. Robust fix is a new open question (below).

## Notes
- The static Workspace `Forest_A` is an **authoring/reference copy only** — the game clones from the ServerStorage template at runtime, so the Workspace copy isn't required. Left at the corridor origin for visual alignment; it will **overlap the first generated segment during Play**, so delete/disable it before publishing (or repurpose it as a static segment 0).
- `GliderHandler` distance tracking already compensates for treadmill recycles via `RunOffsetApplied`.
