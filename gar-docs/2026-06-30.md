# Tomorrow — 2026-06-30

## Top Priority

Build the three Forest segment variations in Roblox Studio to prove out ProcGen v1.

→ [[references/forest-segment-authoring|Forest Segment Authoring Guide]]

## What the Code Agent Did (needs Studio side to match)

The `ProcGenManager.server.lua` handoff is written and ready to implement. It expects:

- `ServerStorage/SegmentTemplates/Forest/Forest_A` (Model)
- `ServerStorage/SegmentTemplates/Forest/Forest_B` (Model)
- `ServerStorage/SegmentTemplates/Forest/Forest_C` (Model)

Each 500 studs deep along +Z, pivot at entry face, rings tagged `PoofRing`.

See the authoring guide above for step-by-step on Creator Store assets, Studio parts, and Blender imports.

## Order of Operations

1. Read the [[references/forest-segment-authoring|authoring guide]] first (5 min)
2. Set up the `ServerStorage/SegmentTemplates/Forest/` folder structure in Studio
3. Build Forest_A ("The Arch") — easiest, get the pivot workflow right on this one
4. Build Forest_B ("The Weave") and Forest_C ("The Descent")
5. Run the Code agent with the ProcGen v1 handoff prompt (`gar-docs/sessions/2026-06-29-procgen-handoff.md`)
6. Playtest — fly through 2+ segment boundaries and verify seamless teleport + ring firing
